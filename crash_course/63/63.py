# Checking for Inequality
# print a message if the person did not order anchovies:x
requested_topping = 'mushrooms'

if requested_topping != 'anchovies': #1
    print("Hold the anchovies")

# 1 - compares the value, if the values do not match, Python returns True and executes the
# following code. If the two values match, Python returns False and does not run the following code.

# Most of the conditional expressions you write will test for equality, but sometimes you'll find it
# more efficient to test for inequality.
