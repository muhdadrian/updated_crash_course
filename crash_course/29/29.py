# Sorting a list permanently with the sort() method - alphabetically

cars = ['bmw', 'audi', 'toyota', 'subaru']
cars.sort() # changes the order of the list permanently. The cars are now in alphabetical order, and we can never revert to the original order.
print(cars)