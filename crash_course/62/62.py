# The lower() function doesn't change the value that was originally stored in car, so you can do this
# kind of comparison without affecting the original variable

car = 'Audi'
print(car.lower() == 'audi')
print(car)

