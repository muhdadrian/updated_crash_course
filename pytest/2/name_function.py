# Testing a Function

def get_formatted_name(first, last):
    """Generate a neatly formatted full name."""
    full_name = f"{first} {last}" 
    return full_name.title()

# to check that get_formatted_name() works, let's make a program that uses this function. Go to names.py 